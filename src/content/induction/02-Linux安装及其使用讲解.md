---
title: "Linux 安装与环境部署讲解"
description: "Ubuntu 双系统安装与环境部署的核心思路与外部资料导航 —— 分区、Secure Boot、高频避坑与显卡驱动。"
author: "视觉组"
date: 2026-08-10
tags: ["Linux", "Ubuntu", "环境配置", "双系统"]
status: done
draft: false
---

> 我想给所有愿意参与到浙江大学RoboMaster战队视觉组培训的同学说：
> **争者留其名**


在进入 RoboMaster 视觉组的实质性开发之前，你需要具备一个合格的系统环境。本篇文档是关于 Linux 环境部署的第一部分。

文档不会提供长篇大论的手把手教学，而是提供核心思路与经过筛选的外部资料导航。请保持耐心，结合自身设备情况进行主动学习。

## 1. 为什么我们选择 Ubuntu？

对于视觉算法开发和机器人工程而言，Windows 并非理想的生产力环境。我们统一要求安装 Ubuntu 的主要原因如下：

*   **硬件接口的透明性**：视觉开发需要高频调用工业相机、USB 转串口等模块。Linux 提供了更直接、底层的硬件读写权限，避免了 Windows 下复杂的闭源驱动冲突。
*   **ROS 生态支持**：机器人操作系统（ROS/ROS 2）是 RM 视觉组与底盘电控通信的基础。Ubuntu 是 ROS 官方支持最完善的宿主系统。
*   **编译效能**：C++ 项目（CMake + OpenCV）在 Linux 环境下的包管理（`apt`）和编译链体验更为流畅和规范。

## 2. Ubuntu 24.04 LTS 双系统安装指南

**关于为什么本篇文档没有“一步步图文教程”：**

给个人电脑（尤其是带有 Nvidia 独显的轻薄本或游戏本）安装 Windows + Ubuntu 双系统，是一个高度**硬件特异性**的操作。联想、戴尔、华硕的 BIOS 界面完全不同；部分设备会遇到 Secure Boot（安全启动）拦截，部分设备会因显卡驱动导致安装黑屏。

在这里强制写一套通用步骤只会让你走弯路。因此，你需要做的是：**查阅下方整理的优质社区教程，并结合你自己的电脑型号进行针对性操作**。

### 2.1 装机前置概念理解

在点开外部文档或视频教程前，请先理解以下三个核心概念的底层逻辑。这不仅是装双系统的必经之路，也是防止你误操作导致原有 Windows 数据损毁的常识。

**1. ISO 镜像与启动盘（解决“引导”的问题）**
*  Ubuntu 系统本身是一个巨大的数据包（即 `.iso` 文件）。电脑在日常状态下，无法直接“双击”运行并安装它。
*   **做法**：你需要准备一个空白 U 盘，并借助 Rufus 或 Ventoy 等烧录工具，将 ISO 文件写入其中。这个过程相当于把普通 U 盘改造成了一把**能够独立启动电脑的物理钥匙**。插上它开机，电脑才能跳过你硬盘里的 Windows，直接进入 Ubuntu 的安装环境。

**2. 磁盘分区压缩**
*   在 Windows 自带的“磁盘管理”工具中，强制从现有的磁盘中切出一块“未分配”的空白空间。
*   **为什么要这么做**：你的硬盘就像一块已经被完全划分好的地皮。Ubuntu 是一套全新的系统，它不会和 Windows 挤在同一个分区的房间里。如果你不提前在 Windows 里把地皮切出一块空白区域，Ubuntu 安装程序就找不到空地。**如果在没有空白空间的情况下强行安装，极易导致整个硬盘被意外格式化。**
*   *注：考虑到视觉组后期需要存储大量编译文件、深度学习数据集，内录视频等等。建议此处至少切出 200GB 以上的未分配空间。*

**3. 关闭 Secure Boot / 安全启动**
*   Secure Boot 是主板 BIOS 层面的一项底层安全机制。你可以把它理解为**微软联合主板厂商设置的“系统保护”**。
*   **为什么要这么做**：默认情况下，这个保卫只允许带有微软数字签名的系统（也就是 Windows 本身）启动。如果你不进入主板后台把该选项关掉（设为 `Disabled`），电脑就会把你插上的 Ubuntu U 盘当作非法程序直接拦截，你将永远卡在 Windows 里。此外，如果不关闭该机制，后续安装 Nvidia 闭源显卡驱动时也会被系统拦截，导致开机黑屏。

### 2.2 视频扩展建议（推荐结合视频理解概念和操作）：
 
如果你对上述的“写盘、分区、改 BIOS”空间拓扑逻辑依然感到抽象，请**在开启代理的前提下**，优先观看以下两个 YouTube 教程。看完后，你对装机的全局认知将得到提高：
 
**1. 完整流程实操演示（含 Rufus 与分区操作）**
*   **视频标题**：Dual Boot Windows and Ubuntu Linux in 10 Minutes (2026)
*   **视频链接**：[https://www.youtube.com/watch?v=UTqDuWHbZkw](https://www.youtube.com/watch?v=UTqDuWHbZkw)
*   **学习指南**：这是一个极为干脆的教程。建议从头看到尾，重点观看 `04:12`（使用 Rufus 制作启动 U 盘）、`06:30`（Windows 下检查与压缩分区）以及 `09:46`（Ubuntu 实际安装过程）。
 
**2. “分区原理”与“双系统安全启动”**
*   **视频标题**：Dual-Boot Made Easy: Manual Partitioning
*   **视频链接**：[https://www.youtube.com/watch?v=alMwciirq6A](https://www.youtube.com/watch?v=alMwciirq6A)
*   **学习指南**：如果你在分配 Ubuntu 的根目录（`/`）、交换空间（`swap`）和 EFI 引导区时感到迷茫，请看这个视频。它用极度清晰的画面向你解释了“手动分区”的物理原理。
 
*(注：如果你的当前网络暂无法访问 YouTube，请在 Bilibili 搜索 `Rufus Ventoy 完整使用教程` 与 `双系统 分区原理 Secure Boot`，寻找播放量最高的教程作为平替。)*

### 2.3 硬件特异性与高频避坑指南（重要）

由于各品牌电脑（联想、华硕、戴尔等）的主板设计与出厂预装策略不同，你可能会在进入引导盘或安装过程中遇到特异性阻拦。以下是四个最高频的报错痛点，请对照自身设备进行排查。遇到对应情况时，请将加粗的关键词带入搜索引擎寻找特定教程。

**1. 无法进入 BIOS 或 U 盘引导菜单**
*   **现象**：插上 U 盘重启，电脑依然直接进入 Windows，没有给你选择 U 盘启动的机会。
*   **原因与对策**：各品牌进入 BIOS（底层固件设置）和 Boot Menu（启动菜单）的快捷键不同。例如：联想通常是 `F2` 进入 BIOS，`F12` 选择启动项；华硕通常是 `F2` 和 `Esc`；戴尔通常是 `F2` 和 `F12`。开机时需高频连续敲击对应按键。
*   **搜索建议**：`[你的电脑品牌] 如何进入 BIOS 设置 U 盘启动`

**2. BitLocker 恢复密钥锁死（数据丢失高风险）**
*   **现象**：调整磁盘分区或修改 BIOS 设置后，重启进入 Windows 时，屏幕变成蓝色并要求输入一长串恢复密钥。
*   **原因与对策**：大部分预装 Windows 11 的电脑默认开启了硬盘加密（BitLocker）。主板环境的微小变动都会触发其防篡改机制。**在开始装机前，必须在 Windows 系统中提前备份 BitLocker 恢复密钥（保存到手机或 U 盘）。**
*   **搜索建议**：`Windows 11 如何查找并备份 BitLocker 恢复密钥`

**3. Ubuntu 安装界面找不到电脑固态硬盘 (VMD/RST 问题)**
*   **现象**：成功进入 Ubuntu 安装界面，但在选择安装位置时，系统显示硬盘为空，找不到你切出来的 200G 空间，甚至连原来的 Windows 盘都看不到。
*   **原因与对策**：部分搭载 Intel 较新处理器的笔记本，默认开启了 Intel VMD (RST) 硬盘存储技术，Ubuntu 的内核默认缺少该技术的驱动。你需要在 BIOS 中将 SATA 模式从 `RAID/VMD` 更改为 **`AHCI`**。
*   *注意：直接在 BIOS 中修改会导致 Windows 蓝屏崩溃。必须配合 Windows 的“安全模式”进行无损切换。*
*   **搜索建议**：`双系统 VMD 改 AHCI 无损切换教程`

**4. 带有 Nvidia 独立显卡的电脑卡死/黑屏**
*   **现象**：在通过 U 盘引导，点击 "Install Ubuntu" 之后，屏幕长时间黑屏，或者卡在电脑品牌 Logo 界面死机，光标停止闪烁。
*   **原因与对策**：Ubuntu 内置的开源显卡驱动（Nouveau）与部分较新的 Nvidia 独立显卡存在严重冲突。你需要在 GRUB 引导界面（选择安装 Ubuntu 的那个黑底白字界面）按 `e` 键进入编辑模式，在 `quiet splash` 后面加上 **`nomodeset`** 参数，暂时禁用显卡驱动以完成安装。
*   **搜索建议**：`Ubuntu 安装过程黑屏死机 nomodeset 解决办法`


### 2.4 推荐学习资源（视频与图文）

#### 官方教学文档
🔗 [Install Ubuntu Desktop - Official Tutorial](https://ubuntu.com/tutorials/install-ubuntu-desktop)
很多问题和疑惑都可以在官方文档中找到答案。建议在安装过程中遇到问题的时候询问LLM
> (提示：我也建议在知乎或 CSDN 检索教程时，加上你的电脑品牌，如“Ubuntu 24.04 双系统 联想拯救者”，获取的方案会更精准。)

### 2.5 安装后记：关于显卡驱动

如果顺利进入 Ubuntu 24.04 桌面，说明你已完成基础部署。
对于搭载 Nvidia 独立显卡的电脑，Ubuntu 默认使用开源的 Nouveau 驱动，其图形性能较弱。

**基础设置要求**：
在系统连网后，打开系统自带的 `Software & Updates (软件和更新)`，找到 `Additional Drivers (附加驱动)` 选项卡。选择带有 `(proprietary, tested)` 标识的 Nvidia 闭源驱动并应用。重启电脑后，系统的图形渲染和后续的 CUDA 计算环境才算真正准备就绪。

